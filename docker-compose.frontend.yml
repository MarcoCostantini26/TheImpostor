# ============================================================
# Mac 4 - frontend (172.20.10.7)
# Esegui con: docker compose -f docker-compose.frontend.yml up --build
# ============================================================
services:
  frontend:
    build:
      context: ./front-end
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://172.20.10.2:8080
        VITE_WS_BASE_URL: ws://172.20.10.5:3000
    ports:
      - "5173:5173"

